$(document).ready(function(){
	$("#lastthinglog").html("la carga del encabezado");
$("#log").append("la carga del encabezado");
	$(".nav_menu > ul").dropmenu({
		effect: "slide",
		speed:250,
		timeout: 0,
		nbsp: false
	});
	
});